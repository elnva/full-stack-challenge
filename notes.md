### Frontend Coding Challenge

“We need a React SPA that fetches employees from GraphQL, displays them in a table, supports sorting and pagination, and updates employee status through a mutation.”

## Data model

Employee
    id
    name
    role
    status

Status (Enum)
    ACTIVE, INACTIVE, SUSPENDED