# Copy this file to .env if you want to add local environment variables later.
# This project currently does not require any env values.
# Optional: override the GraphQL endpoint in the future.
VITE_GRAPHQL_URL=/graphql